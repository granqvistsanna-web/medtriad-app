# Plan 06-01: Tab Bar Navigation Structure

---
phase: 06-navigation-study-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - medtriad/app/(tabs)/_layout.tsx
  - medtriad/app/(tabs)/library.tsx
  - medtriad/app/(tabs)/progress.tsx
  - medtriad/app/(tabs)/settings.tsx
autonomous: true

must_haves:
  truths:
    - "Bottom tab bar shows 4 icons (Home, Library, Progress, Settings)"
    - "Active tab is visually highlighted with filled icon"
    - "Navigation between tabs feels instant"
    - "Tapping each tab shows corresponding screen"
  artifacts:
    - path: "medtriad/app/(tabs)/_layout.tsx"
      provides: "4-tab navigation configuration"
      contains: "library|progress|settings"
    - path: "medtriad/app/(tabs)/library.tsx"
      provides: "Library screen placeholder"
      exports: ["default"]
    - path: "medtriad/app/(tabs)/progress.tsx"
      provides: "Progress screen placeholder"
      exports: ["default"]
    - path: "medtriad/app/(tabs)/settings.tsx"
      provides: "Settings screen placeholder"
      exports: ["default"]
  key_links:
    - from: "medtriad/app/(tabs)/_layout.tsx"
      to: "library.tsx, progress.tsx, settings.tsx"
      via: "Tabs.Screen name prop"
      pattern: 'name="(library|progress|settings)"'
---

<objective>
Set up the 4-tab navigation structure with Home, Library, Progress, and Settings tabs.

Purpose: Enable navigation between app sections as foundation for Library, Progress, and Settings screens.
Output: Working tab bar with 4 tabs, each showing a placeholder screen that will be populated in subsequent plans.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-navigation-study-mode/06-CONTEXT.md
@.planning/phases/06-navigation-study-mode/06-RESEARCH.md

# Existing code references
@medtriad/app/(tabs)/_layout.tsx
@medtriad/constants/theme.ts
@medtriad/components/ui/icon-symbol.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tab layout with 4 tabs</name>
  <files>medtriad/app/(tabs)/_layout.tsx</files>
  <action>
Update the existing tab layout to have 4 tabs: Home, Library, Progress, Settings.

1. Replace the "explore" tab with "library"
2. Add "progress" tab after library
3. Add "settings" tab after progress
4. Use SF Symbols with filled/unfilled variants based on `focused` prop:
   - Home: house / house.fill
   - Library: book / book.fill
   - Progress: chart.bar / chart.bar.fill
   - Settings: gearshape / gearshape.fill

Follow the existing pattern from the current `_layout.tsx`. Use `focused` parameter in tabBarIcon to toggle between filled and unfilled icons (active state shows filled).

Reference research Pattern 1 for exact icon names and structure.
  </action>
  <verify>
Run `npx expo start` and verify:
- 4 tabs appear in bottom bar
- Each tab has an icon and label
- Active tab shows filled icon variant
- Tapping tabs navigates between screens
  </verify>
  <done>Tab bar shows Home, Library, Progress, Settings with proper icons and navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder screens</name>
  <files>medtriad/app/(tabs)/library.tsx, medtriad/app/(tabs)/progress.tsx, medtriad/app/(tabs)/settings.tsx</files>
  <action>
Create three new screen files with placeholder content.

For each screen (library.tsx, progress.tsx, settings.tsx):
1. Create functional component with SafeAreaView wrapper
2. Add centered title text matching the tab name ("Library", "Progress", "Settings")
3. Use existing theme constants (Colors.light, Typography, Spacing)
4. Style consistently with existing home screen

Delete the old `explore.tsx` file - it's being replaced by library.tsx.

Structure for each:
```tsx
import { View, Text, StyleSheet } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Colors, Typography, Spacing } from '@/constants/theme';

export default function [ScreenName]Screen() {
  const colors = Colors.light;

  return (
    <SafeAreaView style={[styles.container, { backgroundColor: colors.background }]}>
      <Text style={[styles.title, { color: colors.text }]}>[Screen Name]</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, alignItems: 'center', justifyContent: 'center' },
  title: { ...Typography.title },
});
```
  </action>
  <verify>
Run `npx expo start` and verify:
- Tapping Library tab shows "Library" text
- Tapping Progress tab shows "Progress" text
- Tapping Settings tab shows "Settings" text
- No errors in console about missing screens
  </verify>
  <done>All 4 tabs navigate to their respective screens without errors.</done>
</task>

</tasks>

<verification>
- [ ] `npx expo start` runs without errors
- [ ] 4 tabs visible in bottom bar: Home, Library, Progress, Settings
- [ ] Active tab shows filled icon, inactive tabs show outline icons
- [ ] Tapping each tab shows corresponding screen content
- [ ] Tab bar hidden during quiz (existing fullScreenModal behavior preserved)
- [ ] No TypeScript errors (`npx tsc --noEmit`)
</verification>

<success_criteria>
Four-tab navigation structure working with:
- Home (existing), Library (placeholder), Progress (placeholder), Settings (placeholder)
- Icons toggle between filled/unfilled based on active state
- Instant navigation between tabs
- Ready for content implementation in subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/06-navigation-study-mode/06-01-SUMMARY.md`
</output>
