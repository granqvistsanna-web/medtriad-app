---
phase: 09-ui-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - medtriad/app/quiz/index.tsx
autonomous: true

must_haves:
  truths:
    - "Quiz main area has Spacing.lg (24px) horizontal padding"
    - "Quiz screen visual consistency matches Home patterns"
  artifacts:
    - path: "medtriad/app/quiz/index.tsx"
      provides: "Quiz screen with consistent padding"
      contains: "paddingHorizontal: Spacing.lg"
  key_links:
    - from: "medtriad/app/quiz/index.tsx"
      to: "medtriad/constants/theme.ts"
      via: "Uses Spacing.lg for horizontal padding"
      pattern: "Spacing\\.lg"
---

<objective>
Polish Quiz screen to match Home screen's visual language.

Purpose: Ensure Quiz screen uses consistent spacing tokens as Home (UIPOL-04)
Output: Quiz screen with consistent horizontal padding in main content area
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ui-polish/09-RESEARCH.md

@medtriad/app/quiz/index.tsx
@medtriad/app/(tabs)/index.tsx (reference - Home screen patterns)
@medtriad/constants/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Quiz main area padding to match Home</name>
  <files>medtriad/app/quiz/index.tsx</files>
  <action>
Update Quiz screen main area padding:

1. In styles.main:
   - Change `paddingHorizontal: Spacing.base` -> `paddingHorizontal: Spacing.lg`

2. Keep header padding as `Spacing.base` (header elements are smaller, denser)

Note: The Quiz screen is already well-styled per research. This is a minor alignment to use consistent Spacing.lg (24px) instead of Spacing.base (16px) in the main content area where FindingsCard and AnswerCards are displayed.

The header keeps Spacing.base because:
- Header elements (cancel button, progress, streak, score) are compact
- Different UX pattern than scrollable content screens
- Consistent with iOS navigation bar conventions

Only the main area (question card + answers) gets the wider padding to match Home's content width.
  </action>
  <verify>
Run the app, start a quiz:
- Main content area (FindingsCard, AnswerCards) has wider padding
- Cards don't feel cramped
- Header remains compact with smaller padding
- All quiz functionality works correctly
  </verify>
  <done>Quiz main area uses Spacing.lg (24px) horizontal padding, matching Home content width</done>
</task>

</tasks>

<verification>
After completing task:
1. Start a quiz and compare visual spacing to Home screen
2. FindingsCard and AnswerCards have consistent side margins
3. All quiz functionality preserved (timer, answers, scoring, navigation)
4. Header still looks correct with compact padding
5. Run `npx expo start` - no TypeScript errors
</verification>

<success_criteria>
- Quiz main area uses Spacing.lg (24px) horizontal padding
- Visual consistency with other polished screens
- All existing quiz functionality unchanged
- UIPOL-04 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-polish/09-04-SUMMARY.md`
</output>
