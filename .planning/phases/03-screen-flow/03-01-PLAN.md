---
phase: 03-screen-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - medtriad/app/(tabs)/index.tsx
  - medtriad/components/ui/icon-symbol.tsx
autonomous: true

must_haves:
  truths:
    - "User sees app title 'MedTriads' prominently displayed"
    - "User sees subtitle 'Master medical triads'"
    - "User can tap Start Quiz button to navigate to quiz"
    - "User sees streak stat with flame icon (displays 0)"
    - "User sees high score stat with trophy icon (displays 0)"
    - "User sees total quizzes stat (displays 0)"
  artifacts:
    - path: "medtriad/app/(tabs)/index.tsx"
      provides: "Complete home screen UI replacing template"
      min_lines: 80
    - path: "medtriad/components/ui/icon-symbol.tsx"
      provides: "Icon mappings for flame, trophy, checkmark"
      contains: "flame.fill"
  key_links:
    - from: "app/(tabs)/index.tsx"
      to: "@/components/home/StatsCard"
      via: "import and render"
      pattern: "import.*StatsCard.*from"
    - from: "app/(tabs)/index.tsx"
      to: "@/components/ui/Button"
      via: "import and render"
      pattern: "import.*Button.*from"
---

<objective>
Build the complete home screen with app branding, stats display, and start button.

Purpose: Creates the welcoming entry point to the app, replacing the Expo template with MedTriads branding and stats display.
Output: Polished home screen showing title, subtitle, three stat cards (streak/high score/quizzes), and Start Quiz button.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-screen-flow/03-RESEARCH.md

@medtriad/app/(tabs)/index.tsx
@medtriad/components/home/StatsCard.tsx
@medtriad/components/ui/Button.tsx
@medtriad/components/ui/icon-symbol.tsx
@medtriad/constants/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add icon mappings for stats icons</name>
  <files>medtriad/components/ui/icon-symbol.tsx</files>
  <action>
Add SF Symbol to Material Icons mappings for the home screen stats icons:
- "flame.fill" -> "local-fire-department"
- "trophy.fill" -> "emoji-events"
- "checkmark.circle.fill" -> "check-circle"

These mappings enable the icons to work on both iOS (native SF Symbols) and Android/web (Material Icons fallback).

Update the MAPPING object in icon-symbol.tsx to include these new entries.
  </action>
  <verify>
TypeScript compiles without errors:
```bash
cd medtriad && npx tsc --noEmit
```
  </verify>
  <done>Icon mappings exist for flame.fill, trophy.fill, and checkmark.circle.fill</done>
</task>

<task type="auto">
  <name>Task 2: Build complete home screen</name>
  <files>medtriad/app/(tabs)/index.tsx</files>
  <action>
Replace the Expo template home screen with the MedTriads home screen:

1. **Remove template content:**
   - Remove ParallaxScrollView, HelloWave, and template instructions
   - Keep SafeAreaView as container

2. **Add header section:**
   - App title "MedTriads" using Typography.title at 40pt font size
   - Subtitle "Master medical triads" using Typography.caption
   - Center-aligned with gap

3. **Add stats row:**
   - Use flexDirection: 'row' with gap between cards
   - Three StatsCard components:
     - Streak: value={0}, label="Streak", icon flame.fill with timerWarning color
     - High Score: value={0}, label="Best", icon trophy.fill with timerWarning color
     - Total Quizzes: value={0}, label="Played", icon checkmark.circle.fill with success color
   - Import IconSymbol from '@/components/ui/icon-symbol'
   - Import StatsCard from '@/components/home/StatsCard'

4. **Add footer with Start Quiz button:**
   - Use Button component with label="Start Quiz"
   - onPress navigates to '/quiz' via router.push
   - Position at bottom of screen with padding

5. **Layout structure:**
   ```
   SafeAreaView (flex: 1, backgroundColor)
     View (content - flex: 1, paddingHorizontal, paddingTop, gap)
       View (header - alignItems: center, gap)
         Text (title)
         Text (subtitle)
       View (statsRow - flexDirection: row, gap)
         StatsCard x3
     View (footer - padding, paddingBottom)
       Button
   ```

6. **Use theme values:**
   - Colors from Colors[scheme]
   - Spacing.lg for horizontal padding
   - Spacing.xxl for top padding and gaps
   - Spacing.md for stats row gap

Use the exact layout from 03-RESEARCH.md "Complete Home Screen Structure" section.
  </action>
  <verify>
1. App compiles:
```bash
cd medtriad && npx expo start --clear
```

2. Visual check: Home screen shows:
   - "MedTriads" title centered
   - "Master medical triads" subtitle below
   - Three stat cards in a row (Streak, Best, Played)
   - Start Quiz button at bottom
   - Tapping Start Quiz navigates to quiz
  </verify>
  <done>Home screen displays title, subtitle, stats row with icons, and Start Quiz button. Tapping Start Quiz opens quiz screen.</done>
</task>

</tasks>

<verification>
1. Home screen loads without errors
2. Title "MedTriads" is visible and centered
3. Subtitle "Master medical triads" is visible
4. Three stat cards display with icons:
   - Streak (flame icon, value 0)
   - Best (trophy icon, value 0)
   - Played (checkmark icon, value 0)
5. Start Quiz button is visible at bottom
6. Tapping Start Quiz navigates to quiz screen
7. Dark mode styling works correctly
</verification>

<success_criteria>
- HOME-01: User sees app title "MedTriads" (logo deferred, title sufficient)
- HOME-02: User can tap "Start Quiz" to begin
- HOME-03: User sees streak with flame icon (shows 0)
- HOME-04: User sees high score (shows 0)
- HOME-05: User sees total quizzes completed (shows 0)
</success_criteria>

<output>
After completion, create `.planning/phases/03-screen-flow/03-01-SUMMARY.md`
</output>
