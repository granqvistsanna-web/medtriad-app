---
phase: 09-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - medtriad/app/(tabs)/progress.tsx
  - medtriad/components/progress/StatsCard.tsx
  - medtriad/components/progress/QuizHistoryList.tsx
autonomous: true

must_haves:
  truths:
    - "Progress screen has Spacing.lg (24px) horizontal padding"
    - "StatsCard uses backgroundSecondary and Radius.lg like Home"
    - "Stats and history sections have section headers with decorative line"
    - "Content animates in with FadeInUp stagger"
  artifacts:
    - path: "medtriad/app/(tabs)/progress.tsx"
      provides: "Progress screen with Home-consistent layout and section headers"
      contains: "paddingHorizontal: Spacing.lg"
    - path: "medtriad/components/progress/StatsCard.tsx"
      provides: "Stats cards matching Home StatCard style"
      contains: "backgroundSecondary"
    - path: "medtriad/components/progress/QuizHistoryList.tsx"
      provides: "History list with section header pattern"
      contains: "RECENT QUIZZES"
  key_links:
    - from: "medtriad/app/(tabs)/progress.tsx"
      to: "medtriad/components/progress/StatsCard.tsx"
      via: "Component with consistent card styling"
      pattern: "StatsCard"
---

<objective>
Polish Progress screen to match Home screen's visual language.

Purpose: Ensure Progress screen uses same card styles, layout patterns, and section headers as Home (UIPOL-02)
Output: Progress screen with consistent cards, section headers with decorative lines, and entry animations
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ui-polish/09-RESEARCH.md

@medtriad/app/(tabs)/progress.tsx
@medtriad/app/(tabs)/index.tsx (reference - Home screen patterns)
@medtriad/components/progress/StatsCard.tsx
@medtriad/components/progress/QuizHistoryList.tsx
@medtriad/components/home/StatsGrid.tsx (reference - section header and card patterns)
@medtriad/constants/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Progress screen layout and add section headers</name>
  <files>medtriad/app/(tabs)/progress.tsx</files>
  <action>
Update Progress screen layout to match Home:

1. Change content padding:
   - `paddingHorizontal: Spacing.base` -> `paddingHorizontal: Spacing.lg`
   - Add `paddingTop: Spacing.md`
   - Keep `paddingBottom: Spacing.xxl`
   - Add `gap: Spacing.lg` to content container

2. Update title styling:
   - Remove `marginTop: Spacing.lg` (container padding handles this)
   - Remove `marginBottom: Spacing.lg` (gap handles this)

3. Add section header before stats grid - pattern from StatsGrid:
   ```tsx
   <View style={styles.sectionHeader}>
     <Text style={[styles.sectionTitle, { color: colors.textMuted }]}>YOUR STATS</Text>
     <View style={[styles.sectionLine, { backgroundColor: colors.border }]} />
   </View>
   ```

4. Update statsGrid and statsRow:
   - Change `gap: Spacing.md` to `gap: Spacing.sm` (matches Home grid)

5. Replace historySection marginTop with natural gap flow:
   - Remove `marginTop: Spacing.xl` from historySection

6. Add entry animations (import FadeInUp, Durations):
   - Wrap title in Animated.View with FadeInUp
   - Wrap section header in Animated.View with staggered delay
   - Wrap stats grid in Animated.View with staggered delay
   - Wrap history section in Animated.View with staggered delay

7. Add new styles:
   ```typescript
   sectionHeader: {
     flexDirection: 'row',
     alignItems: 'center',
     gap: Spacing.md,
   },
   sectionTitle: {
     ...Typography.tiny,
     letterSpacing: 1,
   },
   sectionLine: {
     flex: 1,
     height: 1,
   },
   ```

8. Import Animated from react-native-reanimated, FadeInUp, Typography, Durations from theme.
  </action>
  <verify>
Run the app, navigate to Progress tab:
- Horizontal padding matches Home (24px)
- "YOUR STATS" section header with decorative line visible
- Stats grid uses smaller gap (Spacing.sm)
- Content animates in with stagger
  </verify>
  <done>Progress screen has Home-consistent layout with section headers and entry animations</done>
</task>

<task type="auto">
  <name>Task 2: Update StatsCard to match Home StatCard style</name>
  <files>medtriad/components/progress/StatsCard.tsx</files>
  <action>
Update StatsCard to match Home's StatCard styling:

1. Change background color:
   - `colors.backgroundCard` -> `colors.backgroundSecondary`

2. Change border radius:
   - `Radius.md` -> `Radius.lg` (16px like Home)

3. Update card structure to match Home (label on top, value large, description below):
   - Keep icon at top if present
   - Label uses Typography.tiny + uppercase + letterSpacing: 0.5
   - Value uses fontSize: 28, fontWeight: '700' (not Typography.stat)
   - Add optional description prop below value

4. Update layout to left-align instead of center:
   - Remove `alignItems: 'center'`
   - Keep flex: 1

5. Add minHeight: 100 to match Home card size

6. Updated interface:
   ```typescript
   interface StatsCardProps {
     label: string;
     value: string | number;
     icon?: SymbolViewProps['name'];
     description?: string;
   }
   ```

7. Updated styles:
   ```typescript
   card: {
     flex: 1,
     borderRadius: Radius.lg,
     padding: Spacing.base,
     gap: Spacing.xs,
     minHeight: 100,
   },
   label: {
     ...Typography.tiny,
     textTransform: 'uppercase',
     letterSpacing: 0.5,
   },
   value: {
     fontSize: 28,
     fontWeight: '700',
     lineHeight: 34,
   },
   description: {
     ...Typography.footnote,
   },
   ```
  </action>
  <verify>
Run the app, navigate to Progress tab:
- StatsCards have light gray background (#F8F9FA)
- Cards have larger border radius (16px)
- Labels are uppercase with letter spacing
- Values are large and bold (28px)
- Cards match Home screen StatCard appearance
  </verify>
  <done>StatsCard uses backgroundSecondary, Radius.lg, and Home-consistent typography</done>
</task>

<task type="auto">
  <name>Task 3: Update QuizHistoryList with section header pattern</name>
  <files>medtriad/components/progress/QuizHistoryList.tsx</files>
  <action>
Update QuizHistoryList to use section header pattern from Home:

1. Replace title with section header pattern:
   ```tsx
   <View style={styles.headerRow}>
     <Text style={[styles.headerText, { color: colors.textMuted }]}>RECENT QUIZZES</Text>
     <View style={[styles.headerLine, { backgroundColor: colors.border }]} />
   </View>
   ```

2. Update styles:
   - Remove old title style
   - Add new header styles:
   ```typescript
   headerRow: {
     flexDirection: 'row',
     alignItems: 'center',
     gap: Spacing.md,
     marginBottom: Spacing.md,
   },
   headerText: {
     ...Typography.tiny,
     letterSpacing: 1,
   },
   headerLine: {
     flex: 1,
     height: 1,
   },
   ```

3. Keep existing row styling (it works well)

4. Import Typography from theme if not already imported.
  </action>
  <verify>
Run the app, navigate to Progress tab:
- "RECENT QUIZZES" header matches "YOUR STATS" header style
- Both headers have uppercase text with decorative line
- History list renders correctly below header
  </verify>
  <done>QuizHistoryList uses section header pattern with decorative line</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Compare Progress and Home screen visually - cards should look identical
2. Both section headers ("YOUR STATS", "RECENT QUIZZES") have same pattern
3. Entry animations work with stagger effect
4. All stats display correctly (High Score, Accuracy, Games Played, Best Streak)
5. Run `npx expo start` - no TypeScript errors
</verification>

<success_criteria>
- Progress screen uses Spacing.lg (24px) horizontal padding
- StatsCard uses backgroundSecondary (#F8F9FA) and Radius.lg (16px)
- Section headers use "TEXT + decorative line" pattern
- Content animates in with FadeInUp stagger
- UIPOL-02 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-polish/09-02-SUMMARY.md`
</output>
