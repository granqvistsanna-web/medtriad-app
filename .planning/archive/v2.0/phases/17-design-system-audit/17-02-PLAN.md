---
phase: 17-design-system-audit
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - medtriad/components/home/HeroCard.tsx
  - medtriad/components/home/StatsGrid.tsx
  - medtriad/components/home/TierSection.tsx
  - medtriad/components/home/TierBadge.tsx
autonomous: true

must_haves:
  truths:
    - "HeroCard uses solid background with hard borders, no gradient"
    - "HeroCard uses design tokens instead of hardcoded colors"
    - "StatsGrid cards have hard border treatment"
    - "TierSection and TierBadge use design tokens"
  artifacts:
    - path: "medtriad/components/home/HeroCard.tsx"
      provides: "Hard border hero card"
      contains: "borderBottomWidth"
    - path: "medtriad/components/home/StatsGrid.tsx"
      provides: "Hard border stat cards"
      contains: "CardStyle"
  key_links:
    - from: "HeroCard.tsx"
      to: "theme.ts"
      via: "CardStyle import"
      pattern: "import.*CardStyle"
    - from: "StatsGrid.tsx"
      to: "theme.ts"
      via: "CardStyle import"
      pattern: "import.*CardStyle"
---

<objective>
Align HeroCard and home screen components to hard border design system

Purpose: Transform the hero area and stats grid to use the Duolingo-inspired hard border aesthetic, replacing soft gradients and shadows with bold, playful card styling.

Output: HeroCard with solid background and hard borders. StatsGrid cards with hard border treatment. TierSection and TierBadge using design tokens consistently.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-design-system-audit/17-CONTEXT.md
@.planning/phases/17-design-system-audit/17-RESEARCH.md
@.planning/phases/17-design-system-audit/17-01-SUMMARY.md
@medtriad/constants/theme.ts
@medtriad/components/home/HeroCard.tsx
@medtriad/components/home/StatsGrid.tsx
@medtriad/components/home/TierSection.tsx
@medtriad/components/home/TierBadge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply hard border treatment to HeroCard</name>
  <files>medtriad/components/home/HeroCard.tsx</files>
  <action>
Transform HeroCard from soft gradient to hard border aesthetic:

**1. Remove LinearGradient dependency:**
- Remove `import { LinearGradient } from 'expo-linear-gradient';`
- Replace LinearGradient wrapper with a regular View

**2. Apply CardStyle-based styling to the card:**
Replace the gradient background with solid background + hard borders:
```typescript
card: {
  backgroundColor: colors.backgroundSecondary,
  borderWidth: 2,
  borderColor: colors.border,
  borderBottomWidth: 4,
  borderBottomColor: colors.borderStrong,
  borderRadius: Radius.xl,
  ...Shadows.light.sm,
  // Keep existing padding
  paddingVertical: Spacing.xl,
  paddingHorizontal: Spacing.lg,
  alignItems: 'center',
}
```

**3. Replace hardcoded colors with tokens:**
- `'rgba(0, 0, 0, 0.06)'` border -> Remove (now using borderColor)
- `'#FFFFFF'` percentText -> `colors.textInverse`
- Streak pill `'#FFEDD5'` and `'#C2410C'` -> Keep as domain-specific (streak colors)
- Accuracy pill `'#CCFBF1'` and `'#0F766E'` -> Keep as domain-specific (teal variant)

**4. Replace hardcoded spacing/typography:**
- `fontSize: 24` tierName -> use Typography.heading (fontSize: 22) or add new token
- `fontSize: 15` progressMessage -> use Typography.caption
- `fontSize: 13` pill text -> use Typography.footnote
- `fontSize: 11` percentText -> use Typography.tiny
- `paddingHorizontal: 10` percentBadge -> use Spacing.sm (8) or Spacing.md (12)
- `paddingHorizontal: 16` pill -> use Spacing.base
- `paddingVertical: 10` pill -> use Spacing.sm or Spacing.md
- `gap: 6` pill -> use Spacing.xs (4) or Spacing.sm (8)
- `borderRadius: 12` percentBadge -> use Radius.md
- `borderRadius: 24` pill -> use Radius.xl

**5. Update imports:**
Add Shadows to the import from theme.ts if not already imported.

Keep the existing animation, ProgressRing, and TriMascot logic unchanged.
  </action>
  <verify>
Run the app and visually confirm:
1. HeroCard has solid background with hard border (visible bottom border)
2. No gradient visible
3. All text still readable and properly styled
  </verify>
  <done>HeroCard uses solid background, hard borders, and design tokens throughout</done>
</task>

<task type="auto">
  <name>Task 2: Apply hard border treatment to StatsGrid and tier components</name>
  <files>medtriad/components/home/StatsGrid.tsx, medtriad/components/home/TierSection.tsx, medtriad/components/home/TierBadge.tsx</files>
  <action>
**StatsGrid.tsx:**

1. Import CardStyle from theme.ts:
```typescript
import { Colors, Typography, Shadows, Radius, Spacing, Durations, CardStyle } from '@/constants/theme';
```

2. Apply hard border to stat cards:
```typescript
card: {
  ...CardStyle,
  flex: 1,
  padding: Spacing.base,
  gap: Spacing.xs,
  minHeight: 100,
}
```
Remove the inline backgroundColor and shadow since CardStyle provides them.

3. Replace hardcoded typography:
- `fontSize: 28, lineHeight: 34` value -> Consider keeping as-is (stat display) or use Typography.stat with custom lineHeight

4. Use Easings.press for the spring animation:
```typescript
scale.value = withSpring(0.97, Easings.press);
```
Import Easings from theme.ts if not already imported.

**TierSection.tsx:**

Read the file and replace hardcoded values:
- Any hardcoded fontSize with Typography tokens
- Any hardcoded marginTop/spacing with Spacing tokens
- Any hardcoded width with appropriate token or keep if layout-specific

**TierBadge.tsx:**

Read the file and replace hardcoded values:
- `'#5DD4CC'` -> colors.primary (or keep if intentionally lighter variant)
- `'#3BA99C'` -> colors.primaryDark
- Any hardcoded spacing/sizing with tokens
  </action>
  <verify>
Run the app and visually confirm:
1. Stats grid cards have visible hard borders (2px sides, 4px bottom)
2. Cards compress on press (existing animation maintained)
3. TierSection and TierBadge display correctly
  </verify>
  <done>StatsGrid, TierSection, and TierBadge use design tokens and hard border styling</done>
</task>

</tasks>

<verification>
- [ ] HeroCard no longer uses LinearGradient
- [ ] HeroCard has borderBottomWidth: 4 and borderBottomColor
- [ ] StatsGrid cards use CardStyle or equivalent hard border pattern
- [ ] No hardcoded hex colors remain (except domain-specific pill colors)
- [ ] No TypeScript errors: `cd medtriad && npx tsc --noEmit`
- [ ] App renders correctly: `cd medtriad && npx expo start`
</verification>

<success_criteria>
1. HeroCard displays with solid background and hard border treatment
2. StatsGrid cards have consistent hard border styling
3. TierSection and TierBadge use design tokens
4. Visual style is bold and playful (Duolingo-inspired)
5. All existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/17-design-system-audit/17-02-SUMMARY.md`
</output>
