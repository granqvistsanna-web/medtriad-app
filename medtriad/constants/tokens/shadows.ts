/**
 * Shadow Tokens
 *
 * Layer 1 of the design system token architecture.
 * Platform-aware shadow definitions (iOS shadow* props + Android elevation).
 */

export const shadows = {
  sm: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 1 },
    shadowOpacity: 0.06,
    shadowRadius: 3,
    elevation: 2,
  },
  md: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.08,
    shadowRadius: 12,
    elevation: 4,
  },
  lg: {
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 8 },
    shadowOpacity: 0.12,
    shadowRadius: 24,
    elevation: 8,
  },
} as const;

// Type helper
export type ShadowKey = keyof typeof shadows;
export type ShadowStyle = typeof shadows.sm;
